<!DOCTYPE html> <!-- checkpoint -->
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Teacher Dashboard | UniLearn</title>
    <link rel="icon" type="image/svg+xml" href="/images/e-learning.svg">
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Playfair+Display:wght@400;700&display=swap" rel="stylesheet">
    
    <script src="https://cdnjs.cloudflare.com/ajax/libs/html2canvas/1.4.1/html2canvas.min.js"></script>
    
    <style>
        body { font-family: 'Inter', sans-serif; background-color: #f5f7fb; }
        .dashboard-card {
            min-height: 220px;
            transition: transform 0.2s ease, box-shadow 0.2s ease;
        }
        .dashboard-card:hover { transform: translateY(-5px); box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05); }
        /* Light theme sidebar styling */
        .sidebar-link.active { background-color: #eff6ff; color: #2563eb; border-left: 3px solid #2563eb; }
        .sidebar-link:not(.active):hover { background-color: #f3f4f6; color: #374151; }
        .modal { transition: opacity 0.3s ease, transform 0.3s ease; }
        .modal.hidden { opacity: 0; transform: scale(0.95); pointer-events: none; }
        
        /* FIX: CSS d? t? d?ng c?t chu?i sau 2 d�ng */
        .two-line-ellipsis {
            overflow: hidden;
            display: -webkit-box;
            -webkit-line-clamp: 2; /* Thu?c t�nh WebKit */
            line-clamp: 2; /* FIX: Th�m thu?c t�nh chu?n CSS */
            -webkit-box-orient: vertical;
            white-space: normal;
        }

        /* === Styles for Certificate === */
        .certificate { font-family: 'Playfair Display', serif; background-image: radial-gradient(#e6f0ff 1px, transparent 1px); background-size: 20px 20px; position: relative; overflow: hidden; }
        .certificate::before { content: ''; position: absolute; top: 0; left: 0; right: 0; bottom: 0; border: 12px solid transparent; border-image: linear-gradient(45deg, #3b82f6, #3b82f6, #ec4899) 1; pointer-events: none; }
        .certificate-title { font-family: 'Playfair Display', serif; background: linear-gradient(45deg, #3b82f6, #3b82f6); -webkit-background-clip: text; background-clip: text; color: transparent; }
        .certificate-name { font-family: 'Playfair Display', serif; font-weight: 700; font-size: 2.5rem; color: #1e3a8a; }
        .seal { position: absolute; bottom: 30px; right: 40px; width: 120px; height: 120px; }
    </style>
    <script src="/js/darkmode.js"></script>
    <script>
        tailwind.config = {
            darkMode: 'class'
        }
    </script>
    <link rel="stylesheet" href="/css/darkmode-improved.css">
</head>
<body class="transition-colors duration-300 bg-gray-50 dark:bg-gray-900">
    <div class="flex h-screen">
        <!-- Light Theme Sidebar -->
        <div class="w-64 bg-white flex flex-col border-r border-gray-200 shadow-sm">
            <!-- Logo -->
            <div class="p-5 border-b border-gray-100">
                <div class="flex items-center gap-3">
                    <img src="/images/e-learning.svg" alt="UniLearn Logo" class="w-10 h-10">
                    <h1 class="text-xl font-bold text-gray-800">UniLearn Teacher</h1>
                </div>
            </div>
            <!-- Profile Section -->
            <div class="p-5 border-b border-gray-100">
                <div class="flex items-center justify-between group cursor-pointer hover:bg-gray-50 rounded-xl p-2 -m-2 transition-colors" id="profile-header">
                    <div class="flex items-center space-x-3">
                        <img id="teacher-avatar-img" alt="Teacher profile" class="w-10 h-10 rounded-full border-2 border-gray-100">
                        <div>
                            <p id="teacher-name" class="font-semibold text-gray-800">Loading...</p>
                            <p id="teacher-email" class="text-xs text-gray-500">Loading...</p>
                        </div>
                    </div>
                    <i class="fas fa-edit text-gray-400 group-hover:text-blue-500 transition-colors"></i>
                </div>
            </div>
            <!-- Navigation -->
            <nav class="flex-1 overflow-y-auto py-4 px-3">
                <a href="#" class="sidebar-link active flex items-center px-4 py-3 text-gray-600 rounded-xl transition-colors mb-1" data-target="dashboard-page">
                    <i class="fas fa-tachometer-alt w-5 mr-3"></i> Dashboard
                </a>
                <a href="#" class="sidebar-link flex items-center px-4 py-3 text-gray-600 rounded-xl transition-colors mb-1" data-target="students-page">
                    <i class="fas fa-users w-5 mr-3"></i> Students
                </a>
                <a href="#" class="sidebar-link flex items-center px-4 py-3 text-gray-600 rounded-xl transition-colors mb-1" data-target="groups-page">
                    <i class="fas fa-comments w-5 mr-3"></i> Study Groups
                </a>
                <a href="#" class="sidebar-link flex items-center px-4 py-3 text-gray-600 rounded-xl transition-colors mb-1" data-target="quizzes-page">
                    <i class="fas fa-puzzle-piece w-5 mr-3"></i> Quizzes
                </a>
            </nav>
            <!-- Logout -->
            <div class="p-5 border-t border-gray-100">
                <button id="logout-button" class="flex items-center text-gray-600 w-full hover:bg-gray-50 hover:text-red-500 p-3 rounded-xl transition-colors">
                    <i class="fas fa-sign-out-alt w-5 mr-3"></i> Logout
                </button>
            </div>
        </div>

        <!-- EDIT PROFILE SIDE PANEL -->
        <div id="edit-profile-panel" class="fixed inset-y-0 right-0 w-96 bg-white dark:bg-gray-800 shadow-2xl transform translate-x-full transition-transform duration-300 ease-in-out z-50 flex flex-col border-l border-gray-100 dark:border-gray-700">
            <div class="bg-gradient-to-r from-blue-500 to-blue-600 text-white p-6 flex justify-between items-center">
                <h2 class="text-xl font-bold">Edit Profile</h2>
                <button id="close-profile-panel" class="text-white hover:bg-white/20 rounded-xl p-2 transition-colors">
                    <i class="fas fa-times text-xl"></i>
                </button>
            </div>

            <div class="flex-1 overflow-y-auto p-6">
                <form id="edit-profile-form" class="space-y-6">
                    <!-- Avatar Section -->
                    <div class="flex flex-col items-center">
                        <div class="relative mb-4">
                            <img id="edit-avatar-preview" alt="Avatar" class="w-32 h-32 rounded-full border-4 border-blue-100">
                            <label for="avatar-file-input" class="absolute bottom-0 right-0 bg-blue-500 text-white w-10 h-10 rounded-xl flex items-center justify-center cursor-pointer hover:bg-blue-600 transition-colors shadow-lg">
                                <i class="fas fa-camera"></i>
                            </label>
                            <input type="file" id="avatar-file-input" accept="image/*" class="hidden">
                        </div>
                        <div id="avatar-upload-progress" class="hidden w-full">
                            <div class="w-full bg-gray-200 rounded-full h-2">
                                <div id="avatar-progress-bar" class="bg-blue-500 h-2 rounded-full transition-all" style="width: 0%"></div>
                            </div>
                            <p class="text-xs text-gray-500 mt-1 text-center">Uploading...</p>
                        </div>
                    </div>

                    <!-- Name -->
                    <div>
                        <label for="edit-name" class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">Full Name</label>
                        <input type="text" id="edit-name" class="w-full px-4 py-2.5 border border-gray-200 dark:border-gray-600 rounded-xl focus:ring-2 focus:ring-blue-500 focus:border-transparent bg-gray-50 dark:bg-gray-700" required>
                    </div>

                    <!-- Email (readonly) -->
                    <div>
                        <label for="edit-email" class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">Email</label>
                        <input type="email" id="edit-email" class="w-full px-4 py-2.5 border border-gray-200 dark:border-gray-600 rounded-xl bg-gray-100 dark:bg-gray-900 cursor-not-allowed text-gray-500" readonly>
                    </div>

                    <!-- Phone -->
                    <div>
                        <label for="edit-phone" class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">Phone</label>
                        <input type="tel" id="edit-phone" placeholder="(555) 123-4567" class="w-full px-4 py-2.5 border border-gray-200 dark:border-gray-600 rounded-xl focus:ring-2 focus:ring-blue-500 focus:border-transparent bg-gray-50 dark:bg-gray-700">
                    </div>

                    <!-- Location -->
                    <div>
                        <label for="edit-location" class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">Location</label>
                        <input type="text" id="edit-location" placeholder="Ho Chi Minh City, Vietnam" class="w-full px-4 py-2.5 border border-gray-200 dark:border-gray-600 rounded-xl focus:ring-2 focus:ring-blue-500 focus:border-transparent bg-gray-50 dark:bg-gray-700">
                    </div>

                    <!-- Save Button -->
                    <div class="pt-4">
                        <button type="submit" class="w-full bg-blue-500 text-white py-3 rounded-xl hover:bg-blue-600 transition-colors font-medium">
                            <i class="fas fa-save mr-2"></i> Save Changes
                        </button>
                    </div>
                </form>
            </div>
        </div>

        <div class="flex-1 flex flex-col overflow-hidden">
            <!-- DASHBOARD PAGE -->
            <div id="dashboard-page" class="page-content flex-1 flex flex-col overflow-hidden">
                <main class="flex-1 overflow-y-auto p-6 bg-gray-50 dark:bg-gray-900">
                    <!-- Header -->
                    <div class="mb-6">
                        <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4">
                            <div>
                                <h2 id="welcome-message" class="text-2xl font-bold text-gray-800 dark:text-gray-200">Dashboard</h2>
                                <p class="text-gray-500 dark:text-gray-400 mt-1">Manage your courses and track student progress</p>
                            </div>
                            <div class="flex items-center gap-3">
                                <a href="/courses" class="flex items-center gap-2 px-4 py-2.5 text-gray-600 bg-white border border-gray-200 rounded-xl hover:bg-gray-50 transition-colors text-sm font-medium">
                                    <i class="fas fa-eye"></i>
                                    <span>Student's View</span>
                                </a>
                                <button id="add-course-btn" class="bg-blue-500 hover:bg-blue-600 text-white px-5 py-2.5 rounded-xl flex items-center text-sm font-medium transition-colors">
                                    <i class="fas fa-plus mr-2"></i> Add New Course
                                </button>

                                <!-- Dark Mode Toggle -->
                                <label class="relative inline-flex items-center cursor-pointer">
                                    <input type="checkbox" id="darkModeToggle" class="sr-only peer">
                                    <div class="w-11 h-6 bg-gray-200 peer-focus:outline-none peer-focus:ring-2 peer-focus:ring-blue-500 rounded-full peer peer-checked:after:translate-x-full peer-checked:after:border-white after:content-[''] after:absolute after:top-[2px] after:left-[2px] after:bg-white after:border-gray-300 after:border after:rounded-full after:h-5 after:w-5 after:transition-all peer-checked:bg-blue-600"></div>
                                    <span class="ml-2 text-sm font-medium text-gray-500">
                                        <i class="fas fa-moon"></i>
                                    </span>
                                </label>
                            </div>
                        </div>
                    </div>

                    <!-- My Published Courses -->
                    <div id="my-courses-section">
                        <div class="bg-white dark:bg-gray-800 rounded-2xl shadow-sm border border-gray-100 dark:border-gray-700">
                            <div class="p-5 border-b border-gray-100 dark:border-gray-700">
                                <h3 class="text-lg font-semibold text-gray-800 dark:text-gray-200">My Published Courses</h3>
                            </div>
                            <div class="p-5">
                                <div id="courses-list" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                                    <!-- Course cards will be rendered here -->
                                </div>
                            </div>
                        </div>
                    </div>
                </main>
            </div>


            <!-- STUDENTS PAGE (NEW) -->
            <div id="students-page" class="page-content hidden flex-1 flex flex-col overflow-hidden">
                <main class="flex-1 overflow-y-auto p-6 bg-gray-50 dark:bg-gray-900">
                    <!-- Header -->
                    <div class="mb-6">
                        <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4">
                            <div>
                                <h2 class="text-2xl font-bold text-gray-800 dark:text-gray-200">Student Management</h2>
                                <p class="text-gray-500 dark:text-gray-400 mt-1">View and manage all registered students</p>
                            </div>
                        </div>
                    </div>

                    <!-- Students Table Card -->
                    <div class="bg-white dark:bg-gray-800 rounded-2xl shadow-sm border border-gray-100 dark:border-gray-700">
                        <div class="p-5 border-b border-gray-100 dark:border-gray-700">
                            <h3 class="text-lg font-semibold text-gray-800 dark:text-gray-200">All Registered Students</h3>
                        </div>
                        <div class="overflow-x-auto">
                            <table class="min-w-full">
                                <thead>
                                    <tr class="border-b border-gray-100 dark:border-gray-700">
                                        <th class="px-5 py-4 text-left text-xs font-semibold text-gray-500 dark:text-gray-400 uppercase tracking-wider">Name</th>
                                        <th class="px-5 py-4 text-left text-xs font-semibold text-gray-500 dark:text-gray-400 uppercase tracking-wider">Email</th>
                                        <th class="px-5 py-4 text-left text-xs font-semibold text-gray-500 dark:text-gray-400 uppercase tracking-wider">Enrolled Courses</th>
                                    </tr>
                                </thead>
                                <tbody id="students-table-body" class="divide-y divide-gray-100 dark:divide-gray-700">
                                    <tr><td colspan="3" class="px-5 py-8 text-sm text-gray-500 dark:text-gray-400 text-center">
                                        <i class="fas fa-spinner fa-spin text-blue-500 text-xl mb-2"></i>
                                        <p>Loading students...</p>
                                    </td></tr>
                                </tbody>
                            </table>
                        </div>
                    </div>
                </main>
            </div>

            <!-- STUDY GROUPS PAGE -->
            <div id="groups-page" class="page-content hidden flex-1 flex flex-col overflow-hidden">
                <main class="flex-1 overflow-y-auto p-6 bg-gray-50 dark:bg-gray-900">
                    <!-- Header -->
                    <div class="mb-6">
                        <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4">
                            <div>
                                <h2 class="text-2xl font-bold text-gray-800 dark:text-gray-200">Study Groups</h2>
                                <p class="text-gray-500 dark:text-gray-400 mt-1">Create and manage study groups for your courses</p>
                            </div>
                            <button id="create-group-btn" class="bg-blue-500 hover:bg-blue-600 text-white px-5 py-2.5 rounded-xl flex items-center text-sm font-medium transition-colors">
                                <i class="fas fa-plus mr-2"></i> Create Study Group
                            </button>
                        </div>
                    </div>

                    <!-- Groups Container -->
                    <div class="bg-white dark:bg-gray-800 rounded-2xl shadow-sm border border-gray-100 dark:border-gray-700 p-5">
                        <div id="groups-container" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                            <!-- Study groups will be rendered here -->
                            <div class="text-center py-12 col-span-full">
                                <i class="fas fa-spinner fa-spin text-blue-500 text-3xl mb-4"></i>
                                <p class="text-gray-500">Loading study groups...</p>
                            </div>
                        </div>
                    </div>
                </main>
            </div>

            <!-- QUIZZES PAGE - FR4.7: Student Quiz Grades -->
            <div id="quizzes-page" class="page-content hidden flex-1 flex flex-col overflow-hidden">
                <main class="flex-1 overflow-y-auto p-6 bg-gray-50 dark:bg-gray-900">
                    <!-- Header -->
                    <div class="mb-6">
                        <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4">
                            <div>
                                <h2 class="text-2xl font-bold text-gray-800 dark:text-gray-200">Student Quiz Grades</h2>
                                <p class="text-gray-500 dark:text-gray-400 mt-1">Track and review student quiz performance</p>
                            </div>
                            <div class="flex items-center gap-3">
                                <select id="quiz-filter-course" class="px-4 py-2.5 bg-white border border-gray-200 rounded-xl focus:ring-2 focus:ring-blue-500 text-sm text-gray-600">
                                    <option value="all">All Courses</option>
                                </select>
                                <button id="refresh-grades-btn" class="bg-blue-500 hover:bg-blue-600 text-white px-5 py-2.5 rounded-xl flex items-center text-sm font-medium transition-colors">
                                    <i class="fas fa-sync-alt mr-2"></i>Refresh
                                </button>
                            </div>
                        </div>
                    </div>

                    <!-- Content -->
                    <div class="bg-white dark:bg-gray-800 rounded-2xl shadow-sm border border-gray-100 dark:border-gray-700 p-5">
                        <div id="quiz-grades-loading" class="flex justify-center items-center py-16">
                            <div class="text-center">
                                <i class="fas fa-spinner fa-spin text-blue-500 text-3xl mb-4"></i>
                                <p class="text-gray-500">Loading quiz grades...</p>
                            </div>
                        </div>
                        <div id="quiz-grades-container" class="hidden space-y-6"></div>
                        <div id="quiz-grades-empty" class="hidden text-center py-16">
                            <i class="fas fa-clipboard-list text-gray-300 text-6xl mb-4"></i>
                            <p class="text-gray-600 dark:text-gray-400 text-lg">No quiz grades found</p>
                            <p class="text-gray-500 dark:text-gray-500 text-sm mt-2">Students haven't completed any quizzes yet</p>
                        </div>
                    </div>
                </main>
            </div>
        </div>
    </div>

    <div id="course-modal" class="modal hidden fixed inset-0 bg-gray-900/50 backdrop-blur-sm overflow-y-auto h-full w-full flex items-center justify-center z-30">
        <div class="relative bg-white dark:bg-gray-800 rounded-2xl shadow-xl p-8 m-4 max-w-lg w-full border border-gray-100 dark:border-gray-700">
            <h2 id="modal-title" class="text-2xl font-bold mb-6 text-gray-800 dark:text-gray-200">Add New Course</h2>
            <form id="course-form">
                <div class="space-y-5">
                    <div>
                        <label for="title" class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">Course Title</label>
                        <input type="text" id="title" required class="block w-full px-4 py-2.5 border border-gray-200 dark:border-gray-600 rounded-xl focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent bg-gray-50 dark:bg-gray-700">
                    </div>
                    <div>
                        <label for="description" class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">Description</label>
                        <textarea id="description" rows="3" class="block w-full px-4 py-2.5 border border-gray-200 dark:border-gray-600 rounded-xl focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent bg-gray-50 dark:bg-gray-700"></textarea>
                    </div>
                    <div>
                        <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-3">Course Access</label>
                        <div class="flex items-center space-x-6">
                            <label class="flex items-center cursor-pointer">
                                <input type="radio" name="course-access" value="free" id="access-free" class="w-4 h-4 text-blue-600 focus:ring-blue-500">
                                <span class="ml-2 text-sm text-gray-700 dark:text-gray-300 flex items-center">
                                    <i class="fas fa-unlock text-blue-500 mr-1"></i> Free Course
                                </span>
                            </label>
                            <label class="flex items-center cursor-pointer">
                                <input type="radio" name="course-access" value="locked" id="access-locked" checked class="w-4 h-4 text-blue-600 focus:ring-blue-500">
                                <span class="ml-2 text-sm text-gray-700 dark:text-gray-300 flex items-center">
                                    <i class="fas fa-lock text-red-500 mr-1"></i> Pro Only (Locked)
                                </span>
                            </label>
                        </div>
                        <p class="mt-2 text-xs text-gray-500">Free courses are accessible to all users. Locked courses require Pro subscription.</p>
                    </div>
                    <div>
                        <label for="category" class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">Category</label>
                        <select id="category" required class="block w-full px-4 py-2.5 border border-gray-200 dark:border-gray-600 rounded-xl focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent bg-gray-50 dark:bg-gray-700">
                            <option value="">-- Select Category --</option>
                            <option value="Development">💻 Development</option>
                            <option value="Design">🎨 Design</option>
                            <option value="Data Science">📊 Data Science</option>
                            <option value="Marketing">📢 Marketing</option>
                            <option value="AI & ML">🤖 AI & ML</option>
                            <option value="Business">💼 Business</option>
                        </select>
                    </div>
                    <div>
                        <label for="imageUrl" class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">Course Image</label>
                        <div class="space-y-3">
                            <input type="file" id="imageFile" accept="image/*" class="block w-full text-sm text-gray-500 file:mr-4 file:py-2 file:px-4 file:rounded-lg file:border-0 file:text-sm file:font-medium file:bg-blue-50 file:text-blue-600 hover:file:bg-blue-100 transition-colors">
                            <div class="flex items-center space-x-2">
                                <span class="text-xs text-gray-500">or</span>
                                <input type="text" id="imageUrl" placeholder="Enter image URL" class="flex-1 px-4 py-2.5 border border-gray-200 dark:border-gray-600 rounded-xl text-sm focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent bg-gray-50 dark:bg-gray-700">
                            </div>
                            <div id="image-preview" class="hidden mt-2">
                                <img id="preview-img" class="h-32 w-full object-cover rounded-xl border border-gray-200" alt="Preview">
                            </div>
                            <div id="upload-progress" class="hidden">
                                <div class="w-full bg-gray-200 rounded-full h-2">
                                    <div id="progress-bar" class="bg-blue-500 h-2 rounded-full transition-all" style="width: 0%"></div>
                                </div>
                                <p class="text-xs text-gray-500 mt-1">Uploading...</p>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="mt-8 flex justify-end space-x-3">
                    <button type="button" id="cancel-btn" class="px-5 py-2.5 bg-gray-100 text-gray-700 dark:bg-gray-700 dark:text-gray-300 rounded-xl hover:bg-gray-200 dark:hover:bg-gray-600 transition-colors font-medium">Cancel</button>
                    <button type="submit" class="px-5 py-2.5 bg-blue-500 text-white rounded-xl hover:bg-blue-600 transition-colors font-medium">Save Course</button>
                </div>
            </form>
        </div>
    </div>

    <!-- Group Forum Modal -->
    <div id="group-forum-modal" class="fixed inset-0 bg-black bg-opacity-40 backdrop-blur-sm hidden flex items-center justify-center z-[100] p-4">
        <div class="bg-white dark:bg-gray-800 rounded-2xl shadow-2xl w-full max-w-4xl max-h-[85vh] flex flex-col overflow-hidden">
            <!-- Header -->
            <div class="flex items-center justify-between px-8 py-6 border-b border-gray-100 dark:border-gray-700 bg-gradient-to-r from-blue-50 to-indigo-50 dark:from-gray-800 dark:to-gray-800">
                <div class="flex items-center space-x-3">
                    <div class="w-10 h-10 bg-gradient-to-br from-blue-500 to-indigo-600 rounded-xl flex items-center justify-center shadow-lg">
                        <i class="fas fa-comments text-white text-lg"></i>
                    </div>
                    <div>
                        <h3 id="forum-group-title" class="text-xl font-bold text-gray-900 dark:text-white">Group Forum</h3>
                        <p class="text-xs text-gray-500 dark:text-gray-400 mt-0.5">Active discussion</p>
                    </div>
                </div>
                <button id="close-forum-modal" class="text-gray-400 hover:text-gray-600 dark:hover:text-gray-200 hover:bg-gray-100 dark:hover:bg-gray-700 rounded-lg p-2 transition-colors">
                    <i class="fas fa-times text-xl"></i>
                </button>
            </div>

            <!-- Messages Area -->
            <div id="forum-messages-container" class="flex-1 overflow-y-auto p-8 space-y-6 bg-gray-50 dark:bg-gray-900">
                <!-- Messages will be loaded here -->
            </div>

            <!-- Message Input -->
            <div class="p-6 border-t border-gray-100 dark:border-gray-700 bg-white dark:bg-gray-800">
                <div class="flex items-end space-x-4">
                    <div class="flex-1">
                        <textarea 
                            id="new-message-input" 
                            rows="2" 
                            placeholder="Type your message... (Shift+Enter for new line)" 
                            class="w-full px-4 py-3 border border-gray-200 dark:border-gray-600 rounded-xl focus:ring-2 focus:ring-blue-500 focus:border-transparent bg-white dark:bg-gray-700 text-gray-800 dark:text-gray-200 resize-none placeholder-gray-400"
                        ></textarea>
                    </div>
                    <button 
                        id="send-message-btn" 
                        class="px-6 py-3 bg-gradient-to-r from-blue-500 to-blue-600 hover:from-blue-600 hover:to-blue-700 text-white rounded-xl font-medium shadow-lg hover:shadow-xl transition-all duration-200 flex items-center space-x-2 min-w-[100px] justify-center"
                    >
                        <span>Send</span>
                        <i class="fas fa-paper-plane"></i>
                    </button>
                </div>
                <p class="text-xs text-gray-500 dark:text-gray-400 mt-2">
                    <i class="fas fa-info-circle mr-1"></i> Press Enter to send, Shift+Enter for new line
                </p>
            </div>
        </div>
    </div>

    <!-- Shared Utilities -->
    <script src="/js/shared/common-utils.js"></script>
    <script src="/js/shared/auth-utils.js"></script>
    <script src="/js/shared/api-utils.js"></script>

    <!-- Page Script -->
    <script src="/js/pages/teacher-dashboard.js"></script>
</body>
</html>
