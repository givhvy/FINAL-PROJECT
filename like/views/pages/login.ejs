<!--
╔══════════════════════════════════════════════════════════════════════════════╗
║  📄 FILE: login.ejs (TRANG ĐĂNG NHẬP - LOGIN PAGE)                          ║
╟──────────────────────────────────────────────────────────────────────────────╢
║  🎯 MỤC ĐÍCH: Form đăng nhập vào hệ thống                                   ║
║  👀 AI DÙNG: Người dùng chưa đăng nhập                                      ║
╟──────────────────────────────────────────────────────────────────────────────╢
║  🔌 API CALLS (Front-end gọi Backend):                                       ║
║     ✅ POST /api/auth/login → Đăng nhập với email + password               ║
║           Request body: { email, password }                                  ║
║           Response: { token, user: {...} }                                   ║
╟──────────────────────────────────────────────────────────────────────────────╢
║  📋 CHỨC NĂNG CHÍNH:                                                         ║
║     ✅ Form đăng nhập với email + password                                  ║
║     ✅ Validation input (check email format, password length)               ║
║     ✅ Hiển thị error messages                                              ║
║     ✅ Lưu token vào localStorage sau khi login thành công                 ║
║     ✅ Redirect dựa theo role:                                              ║
║        - Admin → /admin (admin dashboard)                                   ║
║        - Teacher → /teacher (teacher dashboard)                             ║
║        - Student → /courses (courses page)                                  ║
║     ✅ Link đến trang Sign Up                                               ║
║     ✅ Google OAuth login button (tùy chọn)                                 ║
╟──────────────────────────────────────────────────────────────────────────────╢
║  💡 GIẢI THÍCH CHO TRẺ 5 TUỔI:                                              ║
║     Trang này giống như CỬA VÀO NHÀ 🚪                                      ║
║     - Bạn phải gõ cửa (nhập email + password)                              ║
║     - Nếu đúng chủ nhà → được vào (redirect to dashboard)                  ║
║     - Nếu sai → báo lỗi "Sai mật khẩu rồi!"                                ║
║     - Chưa có tài khoản? → Đi đăng ký ở trang bên cạnh (Sign Up)          ║
╚══════════════════════════════════════════════════════════════════════════════╝
-->
<!DOCTYPE html> <!-- checkpoint -->
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login | UniLearn</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="icon" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 24 24'><path fill='%233B82F6' d='M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5'></path></svg>">
    <style>
        /* 🎨 CSS TỰ CHỈNH - Viết style riêng ngoài Tailwind CSS */

        /* 📝 Cấu hình font cho toàn bộ body */
        body {
            font-family: 'Poppins', sans-serif;  /* Font chữ Poppins (modern, đẹp) */
            background-color: #f0f9ff;          /* Nền xanh nhạt (RGB 240, 249, 255) */
        }

        /* 🎨 Container bên trái: gradient xanh tím (2D graphic area) */
        .login-container {
            background: linear-gradient(135deg, #000000 0%, #4f46e5 100%); 
            /* Gradient từ xanh (6366f1) → tím (4f46e5), angle 135 độ = chéo từ trên-trái xuống dưới-phải */
        }

        /* 📦 Container form bên phải: thêm shadow để nhìn "nổi lên" */
        .form-container {
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1); 
            /* Shadow: offset dọc +10px, mờ 25px, màu đen 10% trong suốt */
            /* Giống như bóng của một tấm bảng gỗ */
        }

        /* ✨ Khi nhấn vào input field: thay đổi màu viền + bóng */
        .input-field:focus {
            border-color: #4f46e5;              /* Viền đổi sang xanh tím */
            box-shadow: 0 0 0 3px rgba(99, 102, 241, 0.2); 
            /* Tạo halo xung quanh field khi focus (giống như bóng nước) */
        }

        /* 🔘 Button styling - gradient + animation khi hover */
        .btn-primary {
            background: linear-gradient(135deg, #6366f1 0%, #4f46e5 100%); 
            /* Gradient xanh tím giống login-container */
            transition: all 0.3s ease; /* Chuyển động mượt 0.3 giây */
        }

        /* 🎬 Khi hover vào button: nâng lên + thêm shadow */
        .btn-primary:hover {
            transform: translateY(-2px);        /* Nâng lên 2px */
            box-shadow: 0 4px 12px rgba(79, 70, 229, 0.3); 
            /* Thêm bóng để nhìn như "floating" */
        }

        /* 🪟 Modal (pop-up dialog) animation */
        .modal { 
            transition: opacity 0.3s ease, transform 0.3s ease; 
            /* Fade in/out + scale animation */
        }

        /* ❌ Khi modal có class 'hidden': ẩn đi */
        .modal.hidden { 
            opacity: 0;              /* Trong suốt hoàn toàn */
            transform: scale(0.95);  /* Nhỏ lại 95% (tạo cảm giác "collapse") */
            pointer-events: none;    /* Không bắt sự kiện chuột */
        }
    </style>
    <script src="/js/darkmode.js"></script>
    <script>
        tailwind.config = {
            darkMode: 'class'
        }
    </script>
    <link rel="stylesheet" href="/css/darkmode-improved.css">
</head>
<body class="min-h-screen flex items-center justify-center p-4 transition-colors duration-300 dark:bg-gray-900">
    <!-- 
    ╔══════════════════════════════════════════════════════════════════════════════╗
    ║  📱 BODY CONTAINER - Hai cột layout: left (decoration) + right (form)       ║
    ╚══════════════════════════════════════════════════════════════════════════════╝
    
    Body content:
    - min-h-screen = chiếm ít nhất 100vh (full viewport height)
    - flex = Flexbox layout (xếp hàng)
    - items-center = Căn giữa theo chiều dọc
    - justify-center = Căn giữa theo chiều ngang
    - p-4 = Padding 1rem ở 4 phía (cách 4 cạnh)
    - transition-colors duration-300 = Chuyển màu mượt trong 300ms (khi dark mode bật)
    - dark:bg-gray-900 = Nền xám đen khi dark mode bật
    -->
    
    <div class="w-full max-w-4xl flex rounded-2xl overflow-hidden shadow-2xl">
        <!-- 
        Container chính: 
        - w-full max-w-4xl = rộng 100% nhưng tối đa 4xl (56rem)
        - flex = Xếp 2 cột
        - rounded-2xl = Góc tròn 2xl
        - overflow-hidden = Cắt bớt phần thừa (giữ corner radius)
        - shadow-2xl = Bóng rất đậm (giống cái hộp 3D)
        -->

        <!-- 🎨 CỘT TRÁI - Hiệu ứng graphic & branding (chỉ hiện desktop) -->
                 <!-- 
            - hidden = ẩn mặc định
            - md:flex = hiện thành flexbox từ tablet trở lên (≥768px)
            - md:w-1/2 = chiếm 50% width từ tablet (trên mobile toàn trang là form)
            - p-8 = padding 2rem (32px) ở 4 phía
            - flex-col = xếp dọc (thay vì ngang)
            - justify-between = khoảng cách giữa items (top/bottom/middle)
            - text-white = chữ trắng
            - relative = positioning context cho children
            -->
        <div class="login-container hidden md:flex md:w-1/2 p-8 flex-col justify-between text-white relative">
    

            <div class="mb-8">
                <!-- Logo section ở trên cùng -->
                <h1 class="text-3xl font-bold mb-2">UniLearn</h1>
                <p class="opacity-80">Your gateway to knowledge</p>
            </div>

            <div class="flex-grow flex items-center justify-center">
                <!-- 
                flex-grow = chiếm hết không gian còn lại (giữa logo trên & copyright dưới)
                flex items-center justify-center = Căn giữa image
                -->
                <img src="/images/c22418cb-bb20-47d2-8715-050315f1d16b.png" alt="Login Illustration" class="w-64 h-64 object-contain">
                <!-- 
                Image decoration bên trái:
                - w-64 h-64 = 16rem × 16rem (256px × 256px)
                - object-contain = Giữ tỉ lệ ảnh (không bóp méo)
                -->
            </div>

            <div>
                <!-- Copyright ở dưới cùng -->
                <p class="text-sm opacity-70">&copy; <%= new Date().getFullYear() %> UniLearn. All rights reserved.</p>
            </div>
        </div>

        <!-- 📋 CỘT PHẢI - Form đăng nhập (main content) -->
        <div class="w-full md:w-1/2 bg-white dark:bg-gray-800 p-8 form-container">
            <!-- 
            - w-full md:w-1/2 = chiếm 100% trên mobile, 50% từ tablet
            - bg-white dark:bg-gray-800 = nền trắng, hoặc xám đậm khi dark mode
            - p-8 = padding 2rem
            - form-container = CSS class với shadow
            -->

            <!-- 📝 Tiêu đề & subtitle -->
            <div class="text-center mb-8">
                <h2 class="text-2xl font-bold text-gray-800 dark:text-gray-200">Welcome Back!</h2>
                <p class="text-gray-600 dark:text-gray-400">Please sign in to continue learning</p>
            </div>

            <!-- 📝 FORM ĐĂNG NHẬP - Các input fields -->
            <form id="loginForm" class="space-y-6">
                <!-- space-y-6 = khoảng cách giữa các form items là 1.5rem (24px) -->

                <!-- 📧 EMAIL FIELD -->
                <div>
                    <label for="email" class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">
                        Email Address
                        <!-- Label = tên gọi input (click vào đây sẽ focus input) -->
                    </label>
                    <input type="email" id="email" class="input-field w-full px-4 py-3 rounded-lg border border-gray-300 dark:border-gray-600 focus:outline-none transition-all" placeholder="your@email.com" required>
                    <!-- 
                    Input email:
                    - type="email" = kiểu email (browser tự validate format)
                    - id="email" = định danh (liên kết với label)
                    - class="input-field..." = styling (padding, border, focus effect)
                    - placeholder = gợi ý văn bản nhạt
                    - required = bắt buộc phải điền
                    -->
                </div>

                <!-- 🔐 PASSWORD FIELD -->
                <div>
                    <div class="flex justify-between items-center mb-1">
                        <label for="password" class="block text-sm font-medium text-gray-700 dark:text-gray-300">
                            Password
                        </label>
                        <a href="#" id="forgotPasswordLink" class="text-sm text-indigo-600 hover:text-indigo-800">
                            Forgot Password?
                            <!-- Link "Quên mật khẩu" bên phải label -->
                        </a>
                    </div>
                    <input type="password" id="password" class="input-field w-full px-4 py-3 rounded-lg border border-gray-300 dark:border-gray-600 focus:outline-none transition-all" placeholder="Enter your password" required>
                    <!-- type="password" = ẩn ký tự khi gõ (thay bằng *) -->
                </div>

                <!-- ✅ REMEMBER ME CHECKBOX -->
                <div class="flex items-center">
                    <input type="checkbox" id="remember" class="h-4 w-4 text-indigo-600 focus:ring-indigo-500 border-gray-300 dark:border-gray-600 rounded">
                    <!-- Checkbox: ô vuông có thể tích (checked/unchecked) -->
                    <label for="remember" class="ml-2 block text-sm text-gray-700 dark:text-gray-300">
                        Remember me
                    </label>
                </div>

                <!-- 🔘 NÚT SUBMIT - "Sign In" -->
                <button type="submit" class="btn-primary w-full py-3 px-4 rounded-lg text-white font-medium focus:outline-none">
                    Sign In
                    <!-- 
                    Button submit:
                    - type="submit" = gửi form khi nhấn
                    - w-full = rộng 100% của parent
                    - py-3 px-4 = padding 12px dọc, 16px ngang
                    - btn-primary = CSS class với gradient + hover effect
                    -->
                </button>
            </form>

            <!-- 🔀 DIVIDER LINE - Dòng tách giữa form & OAuth -->
            <div class="relative my-6">
                <div class="absolute inset-0 flex items-center">
                    <div class="w-full border-t border-gray-300 dark:border-gray-600"></div>
                    <!-- inset-0 = fill toàn parent, border-t = viền trên (dòng nằm ngang) -->
                </div>
                <div class="relative flex justify-center text-sm">
                    <span class="px-2 bg-white dark:bg-gray-800 text-gray-500">
                        Or continue with
                        <!-- Text "Or continue with" nằm giữa dòng (cover dòng border) -->
                    </span>
                </div>
            </div>

            <!-- 🔗 GOOGLE SIGN IN - OAuth button -->
            <a href="/api/auth/google" class="w-full flex items-center justify-center gap-3 px-4 py-3 border border-gray-300 dark:border-gray-600 rounded-lg hover:bg-gray-50 dark:hover:bg-gray-700 transition-colors">
                <!-- 
                Google OAuth:
                - href="/api/auth/google" = link tới backend OAuth (backend sẽ redirect Google login)
                - flex items-center gap-3 = xếp icon + text ngang, cách nhau 12px
                - hover:bg-gray-50 = nền xám nhạt khi hover
                -->
                <svg class="w-5 h-5" viewBox="0 0 24 24">
                    <!-- Google logo SVG -->
                    <path fill="#4285F4" d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"/>
                    <path fill="#34A853" d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"/>
                    <path fill="#FBBC05" d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.07H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.93l2.85-2.22.81-.62z"/>
                    <path fill="#EA4335" d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.07l3.66 2.84c.87-2.6 3.3-4.53 6.16-4.53z"/>
                </svg>
                <span class="text-gray-700 dark:text-gray-300 font-medium">Sign in with Google</span>
            </a>

            <!-- 📝 LINK TỚI SIGN UP -->
            <div class="mt-8 text-center">
                <p class="text-gray-600 dark:text-gray-400">
                    Don't have an account? 
                    <a href="/signup" class="text-indigo-600 font-medium hover:text-indigo-800">
                        Sign up
                        <!-- Link tới trang sign up (cho người chưa có tài khoản) -->
                    </a>
                </p>
            </div>

            <!-- ⚠️ ERROR MESSAGE - Thông báo lỗi khi login fail -->
            <div id="errorMessage" class="hidden mt-4 p-3 bg-red-100 border border-red-200 text-red-700 text-sm rounded-lg">
                <!-- 
                Error div:
                - id="errorMessage" = JS sẽ update nội dung & ẩn/hiện div này
                - hidden = ẩn mặc định (chỉ hiện khi có lỗi)
                - bg-red-100 = nền đỏ nhạt
                -->
                <p>An error occurred.</p>
            </div>
        </div>
    </div>

    <!-- 📱 Include modal dialogs (Quên mật khẩu, etc.) từ file khác -->
    <%- include('../partials/login-modals') %>

    <!-- 🎬 JavaScript code (xử lý login logic) -->
    <script src="/js/login.js"></script>
</body>
</html>
